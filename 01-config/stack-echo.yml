version: '3.8'

# ==============================================================================
# ECHO FRAMEWORK
# Version : 7.2
# ==============================================================================
# ARCHITECTURE :
# Ce fichier ne gère QUE la couche applicative "ECHO".
# ==============================================================================

services:

  # ============================================================================
  # SECTION 1 : APPLICATIONS PRINCIPALES
  # ============================================================================

  # 1. Open WebUI (Application IA)
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: echo-webui-core
    restart: unless-stopped
    volumes:
      - echo-webui-data:/app/backend/data
      - /opt/ECHO_VERSION:/app/backend/data/ECHO_VERSION:ro
    # EXPOSITION : Port 3000 pour accès direct, Labels pour accès BunkerWeb
    ports:
      - "3000:8080"
    networks:
      - echo-network
    environment:
      - WEBUI_AUTH=True
      - WEBUI_NAME="ECHO AI"
      # - CSRF_TRUSTED_ORIGINS=https://ui.${ECHO_DOMAIN:-localhost}
      - FORWARDED_ALLOW_IPS=*
      - TZ=Europe/Paris
      # Configuration Reverse Proxy (Doc Officielle)
      - "WEBUI_URL=https://ui.${ECHO_DOMAIN:-localhost}"
      # ATTENTION : Séparateur point-virgule (;) obligatoire pour Open WebUI
      - "CORS_ALLOW_ORIGIN=https://ui.${ECHO_DOMAIN:-localhost};https://www.${ECHO_DOMAIN:-localhost};http://localhost:3000;http://127.0.0.1:3000;https://${ECHO_DOMAIN:-localhost};${ECHO_DETECTED_ORIGINS}"
      
      # Sécurité Cookies (Recommandé Doc Officielle)
      - "WEBUI_SESSION_COOKIE_SECURE=true"
      - "WEBUI_AUTH_COOKIE_SECURE=true"
      - "WEBUI_SESSION_COOKIE_SAME_SITE=lax"
      - "WEBUI_AUTH_COOKIE_SAME_SITE=lax"
    
    # Intégration BunkerWeb
    labels:
      - "bunkerweb.SERVER_NAME=ui.${ECHO_DOMAIN:-localhost}"
      - "bunkerweb.AUTO_LETS_ENCRYPT=yes"
      - "bunkerweb.USE_REVERSE_PROXY=yes"
      - "bunkerweb.REVERSE_PROXY_URL=/"
      - "bunkerweb.REVERSE_PROXY_HOST=http://echo-webui-core:8080"
      
      # 1. OPTIMISATIONS STREAMING & UPLOAD
      - "bunkerweb.REVERSE_PROXY_BUFFERING=off"
      - "bunkerweb.REVERSE_PROXY_REQUEST_BUFFERING=no"
      - "bunkerweb.MAX_CLIENT_BODY_SIZE=200m"
      - "bunkerweb.REVERSE_PROXY_READ_TIMEOUT=600"
      - "bunkerweb.REVERSE_PROXY_SEND_TIMEOUT=600"
      
      # 2. WEBSOCKETS
      - "bunkerweb.REVERSE_PROXY_WS=yes"
      
      # 3. RATE LIMITING (Stratégie SPA Robuste)
      - "bunkerweb.USE_LIMIT_REQ=yes"
      
      # Règle 1 : Assets Statiques & Websockets (ILLIMITÉ)
      # Cible tous les dossiers statiques SvelteKit + WS
      - "bunkerweb.LIMIT_REQ_URL_1=^/(_app|static|assets|favicon|ws|socket.io).*"
      - "bunkerweb.LIMIT_REQ_RATE_1=1000r/s"
      
      # Règle 2 : API Générale (Standard)
      - "bunkerweb.LIMIT_REQ_URL=/"
      - "bunkerweb.LIMIT_REQ_RATE=20r/s"
      - "bunkerweb.LIMIT_REQ_BURST=50"

      # 4. SECURITE & WAF (Anti-Ban & Faux Positifs)
      - "bunkerweb.USE_MODSECURITY=yes"
      - "bunkerweb.MODSEC_CRS_PARANOIA_LEVEL=1"
      - "bunkerweb.USE_BAD_BEHAVIOR=yes"
      # EXCLUSION CRITIQUE : Ne pas bannir sur 404 (Assets manquants) ni 429 (Rate Limit)
      - "bunkerweb.BAD_BEHAVIOR_STATUS_CODES=400 401 403 405 444 500 502 503 504"
      
      # Tuning ModSecurity pour API (Prompts IA)
      # Désactivation totale du moteur de règles pour /api/ afin d'éviter les faux positifs sur le contenu des chats (code, urls, etc.)
      - 'bunkerweb.CUSTOM_CONF_MODSEC_openwebui_tuning=SecRule REQUEST_URI "@beginsWith /api/" "id:10002,phase:1,pass,nolog,ctl:ruleEngine=Off"'
      
      # 5. UX & PERFORMANCE
      - "bunkerweb.USE_BROTLI=yes"
      - "bunkerweb.REVERSE_PROXY_INTERCEPT_ERRORS=no"
      
    command: >
      /bin/bash -c "pip install mgzip && ./start.sh"

  # 2. Admin Manager (Dashboard Système ECHO)
  admin-manager:
    # Construction locale pour performance et stabilité (Plus de pip install au boot)
    build:
      context: /opt/docker-admin-manager
      dockerfile: Dockerfile
    image: echo-admin-manager:local
    container_name: echo-admin-manager
    restart: always
    networks:
      - echo-network
    environment:
      - TZ=Europe/Paris
    extra_hosts:
      - "host.docker.internal:host-gateway"
    
    # Intégration BunkerWeb
    labels:
      - "bunkerweb.SERVER_NAME=am.${ECHO_DOMAIN:-localhost}"
      - "bunkerweb.AUTO_LETS_ENCRYPT=yes"
      - "bunkerweb.USE_REVERSE_PROXY=yes"
      - "bunkerweb.REVERSE_PROXY_URL=/"
      - "bunkerweb.REVERSE_PROXY_HOST=http://echo-admin-manager:3001"
      - "bunkerweb.REVERSE_PROXY_INTERCEPT_ERRORS=no"
      - "bunkerweb.MODSEC_CRS_PARANOIA_LEVEL=2"
      
      # Assouplissement WAF pour l'Admin (Burst toléré)
      - "bunkerweb.USE_BAD_BEHAVIOR=yes"
      - "bunkerweb.USE_LIMIT_REQ=yes"
      - "bunkerweb.LIMIT_REQ_RATE=20r/s"
      - "bunkerweb.LIMIT_REQ_BURST=100"
      
      - "bunkerweb.HEADERS_RESPONSE_NAME_1=X-Robots-Tag"
      - "bunkerweb.HEADERS_RESPONSE_VALUE_1=noindex, nofollow"
    
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      # Montage du code en lecture seule pour mises à jour sans rebuild complet si besoin
      - /opt/docker-admin-manager/server.py:/app/server.py:ro
      - /opt/echo-images:/app/static:ro
      - echo-backups:/backups
      - echo-webui-data:/app/backend/data
      - /opt/.owui-secrets:/app/secrets:ro
      - /opt/ECHO_VERSION:/app/ECHO_VERSION:ro
    
    # EXPOSITION : Port 3001 ouvert pour le Reverse Proxy distant
    ports:
      - "3001:3001"

  # ============================================================================
  # SECTION 2 : WORKERS INTERNES (BACKEND)
  # ============================================================================
  
  python-worker:
    image: python:3.11-slim
    container_name: echo-python-worker
    restart: always
    networks:
      - echo-network
    environment:
      - TZ=Europe/Paris
    volumes:
      - /opt/docker-python-worker/worker_api.py:/app/app.py:ro
      - echo-worker-data:/app/data
    command: >
      /bin/bash -c "pip install --no-cache-dir flask flask-cors requests pandas numpy scipy scikit-learn matplotlib seaborn yfinance beautifulsoup4 openpyxl regex sympy && python /app/app.py"

  browser-agent:
    image: python:3.11-slim
    container_name: echo-browser-agent
    restart: always
    networks:
      - echo-network
    ipc: host
    environment:
      - PIP_ROOT_USER_ACTION=ignore
      - TZ=Europe/Paris
    volumes:
      - /opt/docker-browser-agent/browser_api.py:/app/app.py:ro
      - echo-browser-data:/app/data
    command: >
      /bin/bash -c "apt-get update && apt-get install -y chromium fonts-liberation libasound2 libnss3 libgbm1 libnspr4 xdg-utils && pip install --no-cache-dir flask flask-cors DrissionPage html2text && python /app/app.py"

  watchtower:
    image: containrrr/watchtower
    container_name: echo-watchtower
    restart: always
    networks:
      - echo-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Europe/Paris
    command: --interval 3600 --cleanup

  # 3. Redirector (Redirection Root/WWW -> UI)
  redirector:
    image: alpine:latest
    container_name: echo-redirector
    restart: always
    networks:
      - echo-network
    command: tail -f /dev/null
    labels:
      - "bunkerweb.SERVER_NAME=${ECHO_DOMAIN:-localhost} www.${ECHO_DOMAIN:-localhost}"
      - "bunkerweb.AUTO_LETS_ENCRYPT=yes"
      - "bunkerweb.USE_REVERSE_PROXY=no"
      # Redirection 301 Permanente vers le sous-domaine UI (Plugin Natif)
      - "bunkerweb.REDIRECT_TO=https://ui.${ECHO_DOMAIN:-localhost}"
      - "bunkerweb.REDIRECT_TO_REQUEST_URI=yes"
      - "bunkerweb.REDIRECT_TO_STATUS_CODE=301"
      - "bunkerweb.USE_BAD_BEHAVIOR=no"

# ==============================================================================
# NETWORKS & VOLUMES
# ==============================================================================

networks:
  echo-network:
    external: true


volumes:
  echo-webui-data:
    external: true
  echo-worker-data:
    external: true
  echo-browser-data:
    external: true
  echo-backups:
    external: true